import Header from "@/components/header";
import { Box } from "@mui/material";

const Orders = () => {
    return (
        <Box sx={{m: "20px"}}>
            <Header title="ORDERS" subtitle="Check the orders here" />
        </Box>
    )
}

export default Orders;