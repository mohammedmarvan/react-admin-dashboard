import Header from "@/components/header";
import { Box } from "@mui/material";

const Inventory = () => {
    return (
        <Box sx={{m: "20px"}}>
            <Header title="INVENTORY" subtitle="Manage Inventory" />
        </Box>
    )
}

export default Inventory;